streamlit>=1.40
plotly>=5.22
pandas>=2.2
openpyxl>=3.1
python-docx>=1.1
fpdf2>=2.7

# Dépendance pour les fonctionnalités en ligne (chat IA conversationnel) :
anthropic>=0.34

# Dépendance pour la persistance des données en ligne (Supabase) :
requests>=2.31

# Dépendance optionnelle pour la transcription cloud (à activer selon le fournisseur choisi) :
openai>=1.40

# Dépendance optionnelle pour la transcription audio hors-ligne (edge computing) :
# openai-whisper
